.DS_Store
result
*.hm-backup
# herdr writes runtime files (logs, sockets, session + release-note cache)
# into its config dir; only config.toml is ours to track.
home/.config/herdr/*
!home/.config/herdr/config.toml
